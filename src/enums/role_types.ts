import { Locale } from '../lib/types/locale';
import { type Enum } from '../lib/types/enum';

export const role_typesEnum: Enum = {
	admin: { [Locale.ru]: 'Администратор' },
	customer: { [Locale.ru]: 'Покупатель' },
};
