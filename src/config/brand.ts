import { API_BASE_URL } from './constants';
//brand
export const BRAND_LIST = `${API_BASE_URL}/brand/list`;
export const BRAND_DETAIL = `${API_BASE_URL}/brand/{{ID}}`;
export const BRAND_ADD = `${API_BASE_URL}/brand/add`;
export const BRAND_DEL = `${API_BASE_URL}/brand/del`;
export const BRAND_UPD = `${API_BASE_URL}/brand/update`;
